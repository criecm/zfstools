# Outils ZFS

`pra` scripts make equal copies of filesystems (with snapshots, properties… sync)
`sync` makes copies of data only (if you need to keep differents snapshots or ?)
`zfs-program` contains zfs-program(8) lua script(s)

Primary copy is at https://codeberg.org/DSI-ECM/zfstools

